// contains constants

#[allow(dead_code, unused_variables, unused_imports)]

use crate::module::series;
use crate::module::numbers;

pub const PI: f64 = 22.0 / 7.0;
pub const E: f64 = 87.0 / 32.0;
pub static GOLDEN_RATIO: f64 = (1.0 + numbers::sqrt_newton(5.0)) / 2.0;

